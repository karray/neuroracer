# Keys override the defaults of the agent class's keyword arguments ([agent]) and of
# NeuroRacer's ([training]); runs/<config name>/config.json lists every value used.

[agent]
class = "ddpg.Agent"

[training]
env_id = "NeuroRacer-v1"
warmup_steps = 500
